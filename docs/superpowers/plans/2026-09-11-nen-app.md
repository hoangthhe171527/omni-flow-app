# Nền cho cả app — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Người dùng chọn một nền có sẵn cho cả app; nền lưu trên tài khoản (API), hiện ở màn chat và bảng dự án trên app Flutter và web React.

**Architecture:** Bộ 8 nền là bảng TÊN → (gradient sáng/tối + hoạ tiết), giữ hai bản song song ở app (Dart, vẽ bằng `CustomPainter`) và web (CSS gradient + SVG data-URI); thứ đồng bộ là tên. API lưu `users.appearance.background`, đọc qua `/auth/me`, ghi qua `PUT /auth/appearance`. App có `backgroundProvider` (lạc quan + cache máy), widget `SurfaceBackdrop` bọc hai màn; web có `backdropStyle()` áp vào hai khung cuộn.

**Tech Stack:** Laravel 11 + MongoDB (PHPUnit, Pint, chạy trong container `omnicrm-pro-api`); Flutter 3.47 + Riverpod (`flutter test`, `dart format`); React + Zustand + vitest.

**Spec:** `docs/superpowers/specs/2026-09-11-nen-app-design.md`

## Global Constraints

- Tám tên nền, ASCII, giống hệt ở ba kho: `mist`, `ivory`, `walnut`, `felt`, `brass`, `graphite`, `sea`, `dawn`. `null` = mặc định.
- Hoạ tiết alpha ≤ `0.06`. Không chuyển động.
- Tương phản: `OmniColors.mutedForeground` trên CẢ HAI điểm gradient sáng ≥ 4,5:1; `OmniColors.darkMutedForeground` trên cả hai điểm gradient tối ≥ 4,5:1. Đỏ thì chỉnh màu, không nới ngưỡng.
- API: trường `appearance` PHẢI vào `$fillable` của `User` trong cùng commit với endpoint.
- Trước push: api chạy Pint (trong container, `docker cp` tệp test vào trước); app chạy `dart format lib test` rồi `flutter analyze` rồi `flutter test --exclude-tags=live`; web `bun run test` (hoặc `npx vitest run`).
- Công cụ không nằm trên PATH: Flutter `D:\_tools\flutter\bin`; Docker CLI `C:\Users\My laptop\AppData\Local\Programs\DockerDesktop\resources\bin\docker.exe`; Node `D:\_tools\node-v24.20.0-win-x64`; gh `D:\_tools\gh\bin\gh.exe`.
- API test: `docker cp <file> omnicrm-pro-api:/app/<file>` rồi `docker exec -e MONGO_TEST_URI="mongodb://root:password@omnicrm-mongodb:27017/?authSource=admin" omnicrm-pro-api php artisan test --filter=<Class>`. Thư mục `modules/` bind-mount nên mã nguồn sửa là thấy ngay; `tests/` thì không.
- Commit message kết thúc bằng `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; dùng `git commit -F -` với heredoc (backtick trong `-m` bị shell thực thi).

---

## Phần A — API (`d:/_omnicrm/omni-flow-api`)

### Task 1: Trường `appearance` đi từ Mongo ra `/auth/me`

**Files:**
- Create: `modules/Auth/Domain/Backdrops.php`
- Modify: `modules/Identity/Infrastructure/Persistence/Mongo/Models/User.php` (mảng `$fillable`, sau `'notification_prefs',`)
- Modify: `modules/Identity/Application/DTOs/UserDTO.php` (constructor + `toArray`)
- Modify: `modules/Identity/Infrastructure/Persistence/Mongo/Repositories/MongoUserRepository.php` (`toDTO`, sau dòng `notificationPrefs:`)
- Test: `tests/Unit/Auth/BackdropsTest.php`, `tests/Feature/Auth/AppearanceSurvivesTheRoundTripTest.php`

**Interfaces:**
- Produces: `App\Modules\Auth\Domain\Backdrops::NAMES` (`string[]`, 8 phần tử); `UserDTO->appearance` (`array{background: ?string}`); `/auth/me` → `data.user.appearance.background`.

- [ ] **Step 1: Viết bài kiểm đơn vị cho danh sách tên**

```php
<?php
// tests/Unit/Auth/BackdropsTest.php

declare(strict_types=1);

namespace Tests\Unit\Auth;

use App\Modules\Auth\Domain\Backdrops;
use PHPUnit\Framework\TestCase;

/**
 * Nguồn sự thật phía server cho bộ nền: app và web phải dùng ĐÚNG tám tên này.
 */
final class BackdropsTest extends TestCase
{
    public function test_co_dung_tam_ten_khong_trung(): void
    {
        $this->assertCount(8, Backdrops::NAMES);
        $this->assertSame(Backdrops::NAMES, array_values(array_unique(Backdrops::NAMES)));
    }

    public function test_ten_la_ascii_thuong(): void
    {
        foreach (Backdrops::NAMES as $name) {
            $this->assertMatchesRegularExpression('/^[a-z]+$/', $name);
        }
    }

    public function test_biet_ten_nao_hop_le(): void
    {
        $this->assertTrue(Backdrops::isValid('walnut'));
        $this->assertTrue(Backdrops::isValid(null), 'null = mặc định, luôn hợp lệ');
        $this->assertFalse(Backdrops::isValid('go-oc-cho'));
        $this->assertFalse(Backdrops::isValid(''));
    }
}
```

- [ ] **Step 2: Viết bài kiểm vòng tròn cho `/auth/me`** (chỉ phần đọc — endpoint ghi ở Task 2)

```php
<?php
// tests/Feature/Auth/AppearanceSurvivesTheRoundTripTest.php

declare(strict_types=1);

namespace Tests\Feature\Auth;

use Tests\MongoTestCase;

/**
 * Nền chọn được, GHI được và ĐỌC LẠI được qua HTTP và Mongo thật.
 *
 * Cùng họ với NotificationPrefsSurviveTheRoundTripTest: một trường không
 * nằm trong `$fillable` thì `update()` trả 200 rồi lặng lẽ không ghi gì, và
 * chỉ một vòng đi–về thật mới thấy.
 */
final class AppearanceSurvivesTheRoundTripTest extends MongoTestCase
{
    private const PASSWORD = 'Matkhau!2026';

    private function token(): string
    {
        $suffix = uniqid();
        $email = "nen-{$suffix}@example.test";

        $this->postJson('/api/v1/auth/register', [
            'company_name' => "Xưởng {$suffix}",
            'full_name' => 'Quản đốc',
            'email' => $email,
            'password' => self::PASSWORD,
            'password_confirmation' => self::PASSWORD,
        ])->assertCreated();

        return (string) $this->postJson('/api/v1/auth/login', [
            'email' => $email,
            'password' => self::PASSWORD,
            'client_type' => 'mobile',
        ])->assertOk()->json('data.access_token');
    }

    public function test_nguoi_chua_chon_thi_me_tra_null_chu_khong_thieu_khoa(): void
    {
        // App/web đọc `appearance.background`; thiếu cả khoá thì client phải
        // đoán, và một client đoán sai lặng lẽ.
        $token = $this->token();

        $this->withToken($token)
            ->getJson('/api/v1/auth/me')
            ->assertOk()
            ->assertJsonPath('data.user.appearance.background', null);
    }
}
```

- [ ] **Step 3: Chạy hai bài để thấy đỏ**

```bash
cd d:/_omnicrm/omni-flow-api
D="/c/Users/My laptop/AppData/Local/Programs/DockerDesktop/resources/bin/docker.exe"
"$D" cp tests/Unit/Auth/BackdropsTest.php omnicrm-pro-api:/app/tests/Unit/Auth/BackdropsTest.php
"$D" cp tests/Feature/Auth/AppearanceSurvivesTheRoundTripTest.php omnicrm-pro-api:/app/tests/Feature/Auth/AppearanceSurvivesTheRoundTripTest.php
"$D" exec -e MONGO_TEST_URI="mongodb://root:password@omnicrm-mongodb:27017/?authSource=admin" omnicrm-pro-api php artisan test --filter="BackdropsTest|AppearanceSurvivesTheRoundTripTest"
```
Expected: `BackdropsTest` lỗi "Class … Backdrops not found"; bài `me` đỏ vì `data.user.appearance.background` không tồn tại.

- [ ] **Step 4: Tạo `Backdrops`**

```php
<?php
// modules/Auth/Domain/Backdrops.php

declare(strict_types=1);

namespace App\Modules\Auth\Domain;

/**
 * Bộ nền có sẵn của app — nguồn sự thật phía server.
 *
 * Chỉ là TÊN. Màu và hoạ tiết nằm ở client (app vẽ bằng Flutter, web bằng
 * CSS); thứ ba kho phải khớp là danh sách này. Server không cần biết "walnut"
 * trông thế nào, chỉ cần biết nó là một lựa chọn hợp lệ để không lưu rác.
 */
final class Backdrops
{
    /** @var string[] */
    public const NAMES = ['mist', 'ivory', 'walnut', 'felt', 'brass', 'graphite', 'sea', 'dawn'];

    /** null là "mặc định" — luôn hợp lệ. */
    public static function isValid(?string $name): bool
    {
        return $name === null || in_array($name, self::NAMES, true);
    }
}
```

- [ ] **Step 5: Mở `$fillable` và đưa trường qua DTO**

`User.php` — ngay sau `'notification_prefs',`:
```php
        // Cùng lý do với notification_prefs ở trên: thiếu là `update()` nuốt
        // im lặng. Xem AppearanceSurvivesTheRoundTripTest.
        'appearance',
```

`UserDTO.php` — thêm tham số cuối constructor (sau `$notificationPrefs`):
```php
        /**
         * Giao diện người dùng tự chọn; hôm nay chỉ có `background` (tên nền
         * hoặc null). Cùng lý do đi qua DTO như notification_prefs.
         *
         * @var array{background: ?string}
         */
        public array $appearance = ['background' => null],
```
và trong `toArray()` sau `'notification_prefs' => …,`:
```php
            'appearance' => $this->appearance,
```

`MongoUserRepository.php` — trong `toDTO`, sau `notificationPrefs: …,`:
```php
            appearance: [
                'background' => is_array($doc->appearance ?? null)
                    ? (is_string($doc->appearance['background'] ?? null) ? $doc->appearance['background'] : null)
                    : null,
            ],
```

- [ ] **Step 6: Chạy lại, thấy xanh; Pint**

```bash
"$D" exec -e MONGO_TEST_URI="mongodb://root:password@omnicrm-mongodb:27017/?authSource=admin" omnicrm-pro-api php artisan test --filter="BackdropsTest|AppearanceSurvivesTheRoundTripTest"
"$D" exec omnicrm-pro-api vendor/bin/pint --test modules/Auth/Domain/Backdrops.php modules/Identity/Application/DTOs/UserDTO.php modules/Identity/Infrastructure/Persistence/Mongo/Repositories/MongoUserRepository.php modules/Identity/Infrastructure/Persistence/Mongo/Models/User.php tests/Unit/Auth/BackdropsTest.php tests/Feature/Auth/AppearanceSurvivesTheRoundTripTest.php
```
Expected: `Tests: 4 passed`; Pint `PASS`.

- [ ] **Step 7: Commit**

```bash
git add modules/Auth/Domain/Backdrops.php modules/Identity tests/Unit/Auth/BackdropsTest.php tests/Feature/Auth/AppearanceSurvivesTheRoundTripTest.php
git commit -F - <<'MSG'
feat(auth): truong `appearance.background` di tu Mongo ra /auth/me

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
```

### Task 2: `PUT /auth/appearance`

**Files:**
- Modify: `modules/Auth/Interfaces/routes.php` (ngay sau dòng `Route::put('/notification-prefs', …)`)
- Modify: `modules/Auth/Interfaces/Http/Controllers/AuthController.php` (thêm phương thức sau `updateNotificationPrefs`)
- Test: `tests/Feature/Auth/AppearanceSurvivesTheRoundTripTest.php` (thêm 3 bài)

**Interfaces:**
- Consumes: `Backdrops::NAMES`, `Backdrops::isValid` (Task 1).
- Produces: `PUT /auth/appearance` thân `{background: string|null}` → 200 `{success: true, data: {background}}`; tên lạ → 422.

- [ ] **Step 1: Thêm ba bài kiểm ghi/đọc**

Thêm vào lớp `AppearanceSurvivesTheRoundTripTest` (trước dấu `}` cuối):
```php
    public function test_chon_nen_roi_doc_lai_van_thay(): void
    {
        $token = $this->token();

        $this->withToken($token)
            ->putJson('/api/v1/auth/appearance', ['background' => 'walnut'])
            ->assertOk()
            ->assertJsonPath('data.background', 'walnut');

        // Khẳng định đáng giá: thứ đọc được ở lượt sau, không phải phản hồi ghi.
        $this->withToken($token)
            ->getJson('/api/v1/auth/me')
            ->assertOk()
            ->assertJsonPath('data.user.appearance.background', 'walnut');
    }

    public function test_dat_null_la_ve_mac_dinh(): void
    {
        $token = $this->token();
        $this->withToken($token)->putJson('/api/v1/auth/appearance', ['background' => 'sea'])->assertOk();

        $this->withToken($token)
            ->putJson('/api/v1/auth/appearance', ['background' => null])
            ->assertOk();

        $this->withToken($token)
            ->getJson('/api/v1/auth/me')
            ->assertOk()
            ->assertJsonPath('data.user.appearance.background', null);
    }

    public function test_ten_la_bi_tu_choi_va_gia_tri_cu_giu_nguyen(): void
    {
        $token = $this->token();
        $this->withToken($token)->putJson('/api/v1/auth/appearance', ['background' => 'sea'])->assertOk();

        $this->withToken($token)
            ->putJson('/api/v1/auth/appearance', ['background' => 'go-oc-cho'])
            ->assertStatus(422);
        $this->withToken($token)
            ->putJson('/api/v1/auth/appearance', [])
            ->assertStatus(422);

        $this->withToken($token)
            ->getJson('/api/v1/auth/me')
            ->assertOk()
            ->assertJsonPath('data.user.appearance.background', 'sea');
    }
```

- [ ] **Step 2: Chạy để thấy đỏ** (cùng lệnh `docker cp` + `--filter=AppearanceSurvivesTheRoundTripTest` như Task 1 Step 3). Expected: 3 bài mới đỏ với 404.

- [ ] **Step 3: Route + controller**

`routes.php`, ngay sau dòng `notification-prefs`:
```php
        // Nền của app: tự phục vụ như notification-prefs.
        Route::put('/appearance', [AuthController::class, 'updateAppearance']);
```

`AuthController.php`, thêm `use App\Modules\Auth\Domain\Backdrops;` vào khối use, và phương thức sau `updateNotificationPrefs`:
```php
    public function updateAppearance(Request $request, UserRepositoryInterface $users): JsonResponse
    {
        $validated = $request->validate([
            // `present` chứ không `required`: null là một lựa chọn hợp lệ
            // ("mặc định"), còn THIẾU khoá là lỗi của client.
            'background' => ['present', 'nullable', 'string', 'in:'.implode(',', Backdrops::NAMES)],
        ]);

        $userId = $this->actorContext->getEffectiveUserId() ?? $this->actorContext->requireActorId();
        $background = $validated['background'] ?? null;

        // Ghi cả cụm như notification_prefs: driver Mongo không nhận đường dẫn
        // có dấu chấm trong `update`.
        $users->update($userId, [
            'appearance' => ['background' => $background],
        ]);

        return response()->json([
            'success' => true,
            'data' => ['background' => $background],
        ]);
    }
```

- [ ] **Step 4: Chạy lại + Pint** (cùng lệnh; thêm `AuthController.php` và `routes.php` vào Pint). Expected: 5 bài của lớp xanh, Pint PASS.

- [ ] **Step 5: Chạy cả bộ, commit, push, chờ CI**

```bash
"$D" exec -e MONGO_TEST_URI="mongodb://root:password@omnicrm-mongodb:27017/?authSource=admin" omnicrm-pro-api php artisan test 2>&1 | tail -3
git add modules/Auth tests/Feature/Auth/AppearanceSurvivesTheRoundTripTest.php
git commit -F - <<'MSG'
feat(auth): PUT /auth/appearance — chon nen cho ca app, luu tren tai khoan

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
git push origin main
/d/_tools/gh/bin/gh.exe run list --branch main --limit 1
```
Expected: toàn bộ xanh; CI `completed success`.

---

## Phần B — App (`d:/_omnicrm/omni-flow-app`)

Mọi lệnh Flutter: `export PATH="/d/_tools/flutter/bin:$PATH"` trước.

### Task 3: Bộ nền `OmniBackdrops` (token) và bài kiểm tương phản

**Files:**
- Create: `lib/design/tokens/omni_backdrops.dart`
- Modify: `lib/design/tokens/tokens.dart` (thêm `export 'omni_backdrops.dart';`)
- Test: `test/design/omni_backdrops_test.dart`

**Interfaces:**
- Produces: `enum OmniBackdropPattern { none, dots, diagonal, grain }`; `class OmniBackdropSpec { Color top; Color bottom; OmniBackdropPattern pattern; }`; `OmniBackdrops.names: List<String>`; `OmniBackdrops.labelOf(String?) → String?`; `OmniBackdrops.specOf(String? name, Brightness) → OmniBackdropSpec?`; `OmniBackdrops.patternAlpha = 0.06`.

- [ ] **Step 1: Bài kiểm**

```dart
// test/design/omni_backdrops_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:omni_app/design/tokens/contrast.dart';
import 'package:omni_app/design/tokens/tokens.dart';

/// Bộ nền phải đủ nhạt (sáng) / đủ đậm (tối) để chữ vẽ thẳng lên nền còn
/// đọc được, và không được ném lỗi vì một cái tên lạ từ server.
void main() {
  test('có đúng tám nền, không trùng, ASCII thường', () {
    expect(OmniBackdrops.names, hasLength(8));
    expect(OmniBackdrops.names.toSet(), hasLength(8));
    for (final n in OmniBackdrops.names) {
      expect(RegExp(r'^[a-z]+$').hasMatch(n), isTrue, reason: n);
    }
  });

  test('tên nào cũng có nhãn tiếng Việt và spec cho cả hai chế độ', () {
    for (final n in OmniBackdrops.names) {
      expect(OmniBackdrops.labelOf(n), isNotEmpty);
      expect(OmniBackdrops.specOf(n, Brightness.light), isNotNull);
      expect(OmniBackdrops.specOf(n, Brightness.dark), isNotNull);
    }
  });

  test('null và tên lạ → null, KHÔNG ném lỗi', () {
    // Server có thể trả một tên app này chưa biết. Màn chat không được sập
    // vì một tuỳ chọn trang trí.
    expect(OmniBackdrops.specOf(null, Brightness.light), isNull);
    expect(OmniBackdrops.specOf('go-oc-cho', Brightness.dark), isNull);
    expect(OmniBackdrops.labelOf('go-oc-cho'), isNull);
  });

  test('chữ phụ đọc được trên MỌI điểm của MỌI nền, sáng lẫn tối', () {
    // Giờ và vạch ngày trong chat vẽ thẳng lên nền. 4,5:1 là WCAG AA.
    // Bài này đỏ thì CHỈNH MÀU (pha thêm trắng/đen), đừng nới ngưỡng.
    for (final n in OmniBackdrops.names) {
      final light = OmniBackdrops.specOf(n, Brightness.light)!;
      final dark = OmniBackdrops.specOf(n, Brightness.dark)!;
      for (final c in [light.top, light.bottom]) {
        final r = contrastRatio(OmniColors.mutedForeground, c);
        expect(r, greaterThanOrEqualTo(4.5), reason: '$n sáng: ${r.toStringAsFixed(2)}');
      }
      for (final c in [dark.top, dark.bottom]) {
        final r = contrastRatio(OmniColors.darkMutedForeground, c);
        expect(r, greaterThanOrEqualTo(4.5), reason: '$n tối: ${r.toStringAsFixed(2)}');
      }
    }
  });

  test('hoạ tiết mờ, không rối', () {
    expect(OmniBackdrops.patternAlpha, lessThanOrEqualTo(0.06));
  });
}
```

- [ ] **Step 2: Chạy → đỏ** — `flutter test test/design/omni_backdrops_test.dart`. Expected: lỗi biên dịch "OmniBackdrops isn't defined".

- [ ] **Step 3: Token**

```dart
// lib/design/tokens/omni_backdrops.dart
import 'package:flutter/material.dart';

/// Hoạ tiết phủ lên gradient. Alpha cố định ở [OmniBackdrops.patternAlpha].
enum OmniBackdropPattern { none, dots, diagonal, grain }

/// Một nền: gradient trên → dưới + hoạ tiết.
class OmniBackdropSpec {
  const OmniBackdropSpec({
    required this.top,
    required this.bottom,
    required this.pattern,
  });

  final Color top;
  final Color bottom;
  final OmniBackdropPattern pattern;
}

/// Tám nền cho cả app, chọn ở menu tài khoản, lưu trên tài khoản.
///
/// Là TÊN: server lưu `walnut`, app và web mỗi bên dịch tên ra gradient của
/// mình (web: `src/lib/backdrops.ts`). Cái phải đồng bộ là tên —
/// `backdrop_names_match_web_test.dart` giữ.
///
/// Mọi màu đủ nhạt (sáng) / đủ đậm (tối) để chữ phụ vẽ thẳng lên nền đọc
/// được; `omni_backdrops_test.dart` đo bằng WCAG. Thêm nền mà bài kiểm đỏ
/// thì pha thêm trắng/đen vào màu, đừng nới ngưỡng.
abstract final class OmniBackdrops {
  static const double patternAlpha = 0.06;

  static const _labels = <String, String>{
    'mist': 'Sương sớm',
    'ivory': 'Ngà phím',
    'walnut': 'Gỗ óc chó',
    'felt': 'Nỉ búa',
    'brass': 'Dây đồng',
    'graphite': 'Than chì',
    'sea': 'Biển',
    'dawn': 'Rạng đông',
  };

  static const _light = <String, OmniBackdropSpec>{
    'mist': OmniBackdropSpec(top: Color(0xFFEEF6F4), bottom: Color(0xFFE3EFEB), pattern: OmniBackdropPattern.dots),
    'ivory': OmniBackdropSpec(top: Color(0xFFF9F6EE), bottom: Color(0xFFF2ECDE), pattern: OmniBackdropPattern.diagonal),
    'walnut': OmniBackdropSpec(top: Color(0xFFF5ECE4), bottom: Color(0xFFEEE0D3), pattern: OmniBackdropPattern.grain),
    'felt': OmniBackdropSpec(top: Color(0xFFF8EDED), bottom: Color(0xFFF1E0E0), pattern: OmniBackdropPattern.dots),
    'brass': OmniBackdropSpec(top: Color(0xFFF7F2E6), bottom: Color(0xFFEFE7D2), pattern: OmniBackdropPattern.diagonal),
    'graphite': OmniBackdropSpec(top: Color(0xFFF0F2F4), bottom: Color(0xFFE5E9ED), pattern: OmniBackdropPattern.dots),
    'sea': OmniBackdropSpec(top: Color(0xFFEBF3F8), bottom: Color(0xFFDFEAF1), pattern: OmniBackdropPattern.grain),
    'dawn': OmniBackdropSpec(top: Color(0xFFFAF1EB), bottom: Color(0xFFF0E7F0), pattern: OmniBackdropPattern.none),
  };

  static const _dark = <String, OmniBackdropSpec>{
    'mist': OmniBackdropSpec(top: Color(0xFF15211F), bottom: Color(0xFF0F1817), pattern: OmniBackdropPattern.dots),
    'ivory': OmniBackdropSpec(top: Color(0xFF232019), bottom: Color(0xFF191712), pattern: OmniBackdropPattern.diagonal),
    'walnut': OmniBackdropSpec(top: Color(0xFF2A1F18), bottom: Color(0xFF1C1511), pattern: OmniBackdropPattern.grain),
    'felt': OmniBackdropSpec(top: Color(0xFF2B1A1C), bottom: Color(0xFF1E1214), pattern: OmniBackdropPattern.dots),
    'brass': OmniBackdropSpec(top: Color(0xFF2A2416), bottom: Color(0xFF1B1810), pattern: OmniBackdropPattern.diagonal),
    'graphite': OmniBackdropSpec(top: Color(0xFF1B1F24), bottom: Color(0xFF11141A), pattern: OmniBackdropPattern.dots),
    'sea': OmniBackdropSpec(top: Color(0xFF14222B), bottom: Color(0xFF0E181F), pattern: OmniBackdropPattern.grain),
    'dawn': OmniBackdropSpec(top: Color(0xFF2A1E24), bottom: Color(0xFF1A141C), pattern: OmniBackdropPattern.none),
  };

  /// Thứ tự ỔN ĐỊNH — lưới chọn không được nhảy chỗ giữa hai lần mở.
  static List<String> get names => _labels.keys.toList(growable: false);

  static String? labelOf(String? name) => _labels[name];

  /// null hoặc tên lạ → null, KHÔNG ném lỗi: một tuỳ chọn trang trí không
  /// được làm sập màn chat.
  static OmniBackdropSpec? specOf(String? name, Brightness brightness) =>
      name == null ? null : (brightness == Brightness.dark ? _dark : _light)[name];
}
```
Thêm `export 'omni_backdrops.dart';` vào `lib/design/tokens/tokens.dart` (sau `omni_covers.dart`).

- [ ] **Step 4: Chạy → nếu bài tương phản đỏ, pha trắng vào điểm bị nêu tên** (sáng) — mỗi lần tăng mỗi kênh RGB thêm ~6 (0x06) cho tới khi xanh; tối thì trừ. Rồi `dart format lib test` và chạy lại. Expected: `+5: All tests passed!`.

- [ ] **Step 5: Commit**

```bash
git add lib/design/tokens/omni_backdrops.dart lib/design/tokens/tokens.dart test/design/omni_backdrops_test.dart
git commit -F - <<'MSG'
feat(design): OmniBackdrops — tam nen ten theo vat lieu xuong, sang/toi, kiem tuong phan

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
```

### Task 4: Widget `OmniBackdrop` (vẽ gradient + hoạ tiết)

**Files:**
- Create: `lib/design/components/omni_backdrop.dart`
- Modify: `lib/design/components/components.dart` (thêm `export 'omni_backdrop.dart';`)
- Test: `test/design/omni_backdrop_widget_test.dart`

**Interfaces:**
- Consumes: `OmniBackdrops.specOf`, `OmniBackdropSpec`, `OmniBackdropPattern` (Task 3).
- Produces: `OmniBackdrop({required String? name, required Widget child})` — `name == null`/lạ → trả thẳng `child`.

- [ ] **Step 1: Bài kiểm**

```dart
// test/design/omni_backdrop_widget_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:omni_app/design/components/components.dart';
import 'package:omni_app/design/theme/omni_theme.dart';
import 'package:omni_app/design/tokens/tokens.dart';

void main() {
  Widget host(String? name, {bool dark = false}) => MaterialApp(
    theme: OmniTheme.light(TargetPlatform.android),
    darkTheme: OmniTheme.dark(TargetPlatform.android),
    themeMode: dark ? ThemeMode.dark : ThemeMode.light,
    home: OmniBackdrop(name: name, child: const Text('nội dung')),
  );

  Finder gradientBox() => find.byWidgetPredicate(
    (w) => w is DecoratedBox && (w.decoration as BoxDecoration?)?.gradient != null,
  );

  testWidgets('null thì trả thẳng con, không vẽ gì', (tester) async {
    await tester.pumpWidget(host(null));
    expect(find.text('nội dung'), findsOneWidget);
    expect(gradientBox(), findsNothing);
    expect(find.byType(CustomPaint), findsNothing);
  });

  testWidgets('tên lạ cũng như null', (tester) async {
    await tester.pumpWidget(host('go-oc-cho'));
    expect(gradientBox(), findsNothing);
  });

  testWidgets('có tên thì gradient đúng chế độ + hoạ tiết', (tester) async {
    await tester.pumpWidget(host('walnut'));
    final box = tester.widget<DecoratedBox>(gradientBox());
    final g = (box.decoration as BoxDecoration).gradient as LinearGradient;
    expect(g.colors.first, OmniBackdrops.specOf('walnut', Brightness.light)!.top);
    expect(find.byType(CustomPaint), findsOneWidget);

    await tester.pumpWidget(host('walnut', dark: true));
    final dark = tester.widget<DecoratedBox>(gradientBox());
    final gd = (dark.decoration as BoxDecoration).gradient as LinearGradient;
    expect(gd.colors.first, OmniBackdrops.specOf('walnut', Brightness.dark)!.top);
  });

  testWidgets('nền không hoạ tiết thì không có CustomPaint', (tester) async {
    await tester.pumpWidget(host('dawn'));
    expect(gradientBox(), findsOneWidget);
    expect(find.byType(CustomPaint), findsNothing);
  });
}
```

- [ ] **Step 2: Chạy → đỏ** (`OmniBackdrop` chưa có).

- [ ] **Step 3: Widget**

```dart
// lib/design/components/omni_backdrop.dart
import 'package:flutter/material.dart';

import '../tokens/tokens.dart';

/// Nền cả app phía sau một màn "không gian" (chat, bảng dự án).
///
/// `name == null` hoặc tên lạ → trả thẳng [child]: màn giữ nền phẳng như
/// khi chưa có tính năng này. Có tên → gradient theo chế độ sáng/tối của
/// theme hiện tại, phủ hoạ tiết mờ ([OmniBackdrops.patternAlpha]) bằng màu
/// chữ của chế độ. Không chuyển động — nền là nền.
class OmniBackdrop extends StatelessWidget {
  const OmniBackdrop({super.key, required this.name, required this.child});

  final String? name;
  final Widget child;

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    final spec = OmniBackdrops.specOf(name, theme.brightness);
    if (spec == null) return child;

    final inner = spec.pattern == OmniBackdropPattern.none
        ? child
        : CustomPaint(
            painter: _PatternPainter(
              spec.pattern,
              theme.colorScheme.onSurface.withValues(
                alpha: OmniBackdrops.patternAlpha,
              ),
            ),
            child: child,
          );

    return DecoratedBox(
      decoration: BoxDecoration(
        gradient: LinearGradient(
          begin: Alignment.topCenter,
          end: Alignment.bottomCenter,
          colors: [spec.top, spec.bottom],
        ),
      ),
      child: inner,
    );
  }
}

class _PatternPainter extends CustomPainter {
  const _PatternPainter(this.pattern, this.color);

  final OmniBackdropPattern pattern;
  final Color color;

  @override
  void paint(Canvas canvas, Size size) {
    final paint = Paint()
      ..color = color
      ..strokeWidth = 1
      ..style = PaintingStyle.stroke;

    switch (pattern) {
      case OmniBackdropPattern.none:
        return;
      case OmniBackdropPattern.dots:
        final dot = Paint()..color = color;
        for (var y = 12.0; y < size.height; y += 24) {
          for (var x = 12.0; x < size.width; x += 24) {
            canvas.drawCircle(Offset(x, y), 1, dot);
          }
        }
      case OmniBackdropPattern.diagonal:
        // Vạch 45°, cách nhau 28dp, phủ cả góc trên-phải và dưới-trái.
        final span = size.width + size.height;
        for (var d = 0.0; d < span; d += 28) {
          canvas.drawLine(Offset(d, 0), Offset(0, d), paint);
        }
      case OmniBackdropPattern.grain:
        // Vân gỗ: đường ngang hơi lượn, cách 18dp.
        for (var y = 9.0; y < size.height; y += 18) {
          final path = Path()..moveTo(0, y);
          for (var x = 0.0; x < size.width; x += 64) {
            path.quadraticBezierTo(x + 16, y - 3, x + 32, y);
            path.quadraticBezierTo(x + 48, y + 3, x + 64, y);
          }
          canvas.drawPath(path, paint);
        }
    }
  }

  @override
  bool shouldRepaint(_PatternPainter old) =>
      old.pattern != pattern || old.color != color;
}
```
Thêm `export 'omni_backdrop.dart';` vào `lib/design/components/components.dart`.

- [ ] **Step 4: Chạy** `flutter test test/design/omni_backdrop_widget_test.dart test/architecture/design_layer_test.dart` → xanh; `flutter analyze` sạch.

- [ ] **Step 5: Commit** `feat(design): OmniBackdrop — gradient + hoa tiet ve bang ma, theo che do sang/toi`.

### Task 5: Bài kiểm tên khớp web

**Files:**
- Test: `test/design/backdrop_names_match_web_test.dart`

- [ ] **Step 1: Viết bài (bỏ qua khi không có kho web)**

```dart
// test/design/backdrop_names_match_web_test.dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:omni_app/design/tokens/tokens.dart';

/// App và web phải dùng CÙNG bộ tên nền — cùng lý do và cùng cách với
/// `cover_names_match_web_test.dart`.
void main() {
  test('bộ tên nền khớp với src/lib/backdrops.ts bên web', () {
    final web = File('../omni-flow/src/lib/backdrops.ts');
    if (!web.existsSync()) {
      markTestSkipped('Không thấy ../omni-flow/src/lib/backdrops.ts — chỉ chạy khi hai kho nằm cạnh nhau.');
      return;
    }

    // Bảng bên web viết mỗi tên là một khoá: `  walnut: {`
    final names = RegExp(r'^\s{2}([a-z]+):\s*\{', multiLine: true)
        .allMatches(web.readAsStringSync())
        .map((m) => m.group(1)!)
        .toSet();

    expect(names, isNotEmpty, reason: 'Không đọc được tên nào — mẫu khớp lỗi thời?');
    expect(names, OmniBackdrops.names.toSet(),
        reason: 'Thêm nền thì thêm ở CẢ HAI: omni_backdrops.dart và omni-flow/src/lib/backdrops.ts.');
  });
}
```

- [ ] **Step 2: Chạy** — trước Task 13 bài này báo **skipped** (đúng), sau Task 13 phải xanh. Commit: `test(design): ten nen app phai khop web`.

### Task 6: Phiên mang `background`; khoá cache trên máy

**Files:**
- Modify: `lib/core/storage/storage_keys.dart` (thêm `static const background = 'omni.background';` sau `themeMode`)
- Modify: `lib/security/session/session.dart` (`SessionUser`: thêm `this.background` + field)
- Modify: `lib/modules/auth/data/auth_api.dart` (tách `sessionUserFromJson`, parse `appearance.background`)
- Test: `test/auth/session_user_from_json_test.dart`

**Interfaces:**
- Produces: `SessionUser.background: String?`; `SessionUser sessionUserFromJson(Map<String, dynamic> userJson)` (top-level trong `auth_api.dart`); `StorageKeys.background`.

- [ ] **Step 1: Bài kiểm**

```dart
// test/auth/session_user_from_json_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:omni_app/modules/auth/data/auth_api.dart';

/// `/auth/me` trả `appearance.background`; app phải đọc được, và API cũ chưa
/// có khoá thì không văng.
void main() {
  test('đọc appearance.background', () {
    final u = sessionUserFromJson({
      'id': 'u1', 'full_name': 'Hằng Ni', 'email': 'h@x', 'avatar': 'https://x/a.png',
      'appearance': {'background': 'walnut'},
    });
    expect(u.background, 'walnut');
    expect(u.avatarUrl, 'https://x/a.png');
  });

  test('null hoặc thiếu khoá → null', () {
    expect(sessionUserFromJson({'id': 'u1', 'email': 'h@x', 'appearance': {'background': null}}).background, isNull);
    expect(sessionUserFromJson({'id': 'u1', 'email': 'h@x'}).background, isNull);
  });
}
```

- [ ] **Step 2: Chạy → đỏ** (hàm chưa có).

- [ ] **Step 3: Sửa ba tệp**

`session.dart` — `SessionUser`: thêm `this.background,` vào constructor (sau `this.locale`) và field:
```dart
  /// Tên nền cả app người này đã chọn (`OmniBackdrops`), null = mặc định.
  final String? background;
```

`auth_api.dart` — thay khối `user: SessionUser(…)` trong `loadContext()` bằng `user: sessionUserFromJson(userJson),` và thêm hàm top-level cuối tệp:
```dart
/// Một người dùng từ `data.user` của `/auth/me`.
///
/// Tách ra để kiểm được bằng JSON thuần — `loadContext()` gọi hai endpoint
/// và một bài kiểm cho nó phải giả cả hai.
SessionUser sessionUserFromJson(Map<String, dynamic> userJson) => SessionUser(
  id: userJson.strOr('id', ''),
  fullName: userJson.strOr('full_name', userJson.strOr('email', 'Người dùng')),
  email: userJson.strOr('email', ''),
  phone: userJson.str('phone'),
  avatarUrl: userJson.str('avatar'),
  locale: userJson.str('locale'),
  background: userJson.child('appearance').str('background'),
);
```

`storage_keys.dart` — sau `themeMode`:
```dart
  /// Nền cả app đã chọn lần cuối, để mở app không nháy nền trước khi
  /// `/auth/me` về. Nguồn sự thật là tài khoản; đây chỉ là bản chép.
  static const background = 'omni.background';
```

- [ ] **Step 4: Chạy** `flutter test test/auth/session_user_from_json_test.dart test/security` → xanh; `flutter analyze`.

- [ ] **Step 5: Commit** `feat(auth): phien mang appearance.background; khoa cache nen`.

### Task 7: `AppearanceApi.setBackground`

**Files:**
- Create: `lib/modules/settings/data/appearance_api.dart`
- Test: `test/settings/appearance_api_test.dart`

**Interfaces:**
- Produces: `AppearanceApi(ApiClient)`; `Future<void> setBackground(String? name)` → `PUT /auth/appearance {background: name}` (null gửi là JSON null, KHÔNG bỏ khoá).

- [ ] **Step 1: Bài kiểm** (mẫu `_RecordingAdapter` như `test/plans/kpi_month_test.dart`)

```dart
// test/settings/appearance_api_test.dart
import 'dart:convert';
import 'dart:typed_data';

import 'package:dio/dio.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:omni_app/core/network/api_client.dart';
import 'package:omni_app/modules/settings/data/appearance_api.dart';

void main() {
  test('PUT /auth/appearance với đúng khoá `background`', () async {
    final rec = _RecordingAdapter();
    final api = AppearanceApi(ApiClient(Dio()..httpClientAdapter = rec));

    await api.setBackground('walnut');

    final r = rec.requests.single;
    expect(r.method, 'PUT');
    expect(r.uri.path, '/api/v1/auth/appearance');
    expect((r.data as Map)['background'], 'walnut');
  });

  test('null GỬI khoá với giá trị null — bỏ khoá là server trả 422', () async {
    final rec = _RecordingAdapter();
    final api = AppearanceApi(ApiClient(Dio()..httpClientAdapter = rec));

    await api.setBackground(null);

    final body = rec.requests.single.data as Map;
    expect(body.containsKey('background'), isTrue);
    expect(body['background'], isNull);
  });
}

class _RecordingAdapter implements HttpClientAdapter {
  final List<RequestOptions> requests = [];

  @override
  Future<ResponseBody> fetch(RequestOptions options, Stream<Uint8List>? requestStream, Future<void>? cancelFuture) async {
    requests.add(options);
    return ResponseBody.fromString(jsonEncode({'success': true, 'data': {'background': options.data is Map ? (options.data as Map)['background'] : null}}), 200,
        headers: {Headers.contentTypeHeader: [Headers.jsonContentType]});
  }

  @override
  void close({bool force = false}) {}
}
```

- [ ] **Step 2: Chạy → đỏ.**

- [ ] **Step 3: API client**

```dart
// lib/modules/settings/data/appearance_api.dart
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../core/network/api_client.dart';

/// Ghi lựa chọn nền lên tài khoản.
///
/// `null` vẫn phải GỬI (`{"background": null}`): server đòi khoá `present`,
/// bỏ khoá đi là 422 — và "về mặc định" là một lựa chọn thật.
class AppearanceApi {
  AppearanceApi(this._client);

  final ApiClient _client;

  Future<void> setBackground(String? name) =>
      _client.put('/auth/appearance', body: {'background': name});
}

final appearanceApiProvider = Provider<AppearanceApi>(
  (ref) => AppearanceApi(ref.watch(apiClientProvider)),
);
```

- [ ] **Step 4: Chạy → xanh. Commit** `feat(settings): AppearanceApi.setBackground`.

### Task 8: `backgroundProvider` — lạc quan, cache, làm mới phiên

**Files:**
- Create: `lib/modules/settings/application/appearance_providers.dart`
- Test: `test/settings/background_controller_test.dart`

**Interfaces:**
- Consumes: `sessionProvider` (`lib/security/session/session_controller.dart`), `sessionControllerProvider.notifier.refreshContext()`, `preferencesStoreProvider`, `appearanceApiProvider` (Task 7), `StorageKeys.background` (Task 6).
- Produces: `backgroundProvider = NotifierProvider<BackgroundController, String?>`; `BackgroundController.set(String? name)`; ném lại lỗi API sau khi hoàn về giá trị cũ.

- [ ] **Step 1: Bài kiểm**

```dart
// test/settings/background_controller_test.dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:omni_app/core/error/app_exception.dart';
import 'package:omni_app/core/storage/preferences_store.dart';
import 'package:omni_app/core/storage/storage_keys.dart';
import 'package:omni_app/modules/settings/application/appearance_providers.dart';
import 'package:omni_app/modules/settings/data/appearance_api.dart';
import 'package:omni_app/security/session/session.dart';
import 'package:omni_app/security/session/session_controller.dart';
import 'package:shared_preferences/shared_preferences.dart';

/// Nền đọc từ TÀI KHOẢN khi đã đăng nhập, từ cache máy khi chưa; đổi thì
/// hiện ngay, gọi API, lỗi thì hoàn về.
void main() {
  late _FakeApi api;
  late _FakeSessionController sessions;

  Future<ProviderContainer> container({
    Session session = const Session(status: SessionStatus.restoring),
    String? cached,
  }) async {
    SharedPreferences.setMockInitialValues({
      if (cached != null) StorageKeys.background: cached,
    });
    final prefs = PreferencesStore(await SharedPreferences.getInstance());
    api = _FakeApi();
    sessions = _FakeSessionController(session);
    final c = ProviderContainer(
      overrides: [
        preferencesStoreProvider.overrideWithValue(prefs),
        appearanceApiProvider.overrideWithValue(api),
        sessionControllerProvider.overrideWith(() => sessions),
      ],
    );
    addTearDown(c.dispose);
    return c;
  }

  test('chưa có phiên thì lấy cache máy — mở app không nháy nền', () async {
    final c = await container(cached: 'sea');
    expect(c.read(backgroundProvider), 'sea');
  });

  test('có phiên thì tài khoản thắng cache', () async {
    final c = await container(
      cached: 'sea',
      session: Session(
        status: SessionStatus.authenticated,
        user: const SessionUser(id: 'u', fullName: 'H', email: 'h@x', background: 'walnut'),
      ),
    );
    expect(c.read(backgroundProvider), 'walnut');
  });

  test('set: hiện ngay, gọi API, ghi cache, làm mới phiên', () async {
    final c = await container(
      session: Session(
        status: SessionStatus.authenticated,
        user: const SessionUser(id: 'u', fullName: 'H', email: 'h@x'),
      ),
    );
    final future = c.read(backgroundProvider.notifier).set('felt');
    expect(c.read(backgroundProvider), 'felt', reason: 'lạc quan, trước khi API về');
    await future;

    expect(api.sent, ['felt']);
    expect((await SharedPreferences.getInstance()).getString(StorageKeys.background), 'felt');
    expect(sessions.refreshed, 1);
  });

  test('API lỗi thì hoàn về giá trị cũ và ném lại', () async {
    final c = await container(
      session: Session(
        status: SessionStatus.authenticated,
        user: const SessionUser(id: 'u', fullName: 'H', email: 'h@x', background: 'sea'),
      ),
    );
    api.fail = true;

    await expectLater(c.read(backgroundProvider.notifier).set('felt'), throwsA(isA<AppException>()));
    expect(c.read(backgroundProvider), 'sea');
    expect(sessions.refreshed, 0);
  });
}

class _FakeApi implements AppearanceApi {
  final sent = <String?>[];
  bool fail = false;
  @override
  Future<void> setBackground(String? name) async {
    if (fail) throw const NetworkException('Mất mạng rồi');
    sent.add(name);
  }
}

class _FakeSessionController extends SessionController {
  _FakeSessionController(this._session);
  final Session _session;
  int refreshed = 0;
  @override
  Session build() => _session;
  @override
  Future<void> refreshContext() async => refreshed++;
}
```

- [ ] **Step 2: Chạy → đỏ** (provider chưa có). Nếu `SessionController` không cho `build()` ghi đè kiểu này, đọc `lib/security/session/session_controller.dart` và ghi đè đúng chữ ký (`Session build()` là Notifier đồng bộ).

- [ ] **Step 3: Provider**

```dart
// lib/modules/settings/application/appearance_providers.dart
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../core/storage/preferences_store.dart';
import '../../../core/storage/storage_keys.dart';
import '../../../security/session/session.dart';
import '../../../security/session/session_controller.dart';
import '../data/appearance_api.dart';

/// Tên nền cả app đang dùng (null = mặc định).
///
/// Nguồn sự thật là TÀI KHOẢN (`/auth/me`), để đổi máy hay mở web vẫn cùng
/// nền. Cache trên máy chỉ để lúc mở app, trước khi `/auth/me` về, nền không
/// nháy từ phẳng sang có vân. Đổi nền: hiện ngay (lạc quan) → gọi API → ghi
/// cache → làm mới phiên; lỗi thì hoàn về và ném lại cho màn hình báo.
final backgroundProvider = NotifierProvider<BackgroundController, String?>(
  BackgroundController.new,
);

class BackgroundController extends Notifier<String?> {
  @override
  String? build() {
    final session = ref.watch(sessionProvider);
    final store = ref.read(preferencesStoreProvider);

    if (session.status != SessionStatus.authenticated) {
      return store.getString(StorageKeys.background);
    }

    final fromAccount = session.user?.background;
    // Đồng bộ cache theo tài khoản, ngoài khung dựng để không ghi đĩa trong build.
    if (store.getString(StorageKeys.background) != fromAccount) {
      Future<void>.microtask(
        () => store.setString(StorageKeys.background, fromAccount),
      );
    }

    return fromAccount;
  }

  Future<void> set(String? name) async {
    final before = state;
    state = name;
    try {
      await ref.read(appearanceApiProvider).setBackground(name);
      await ref.read(preferencesStoreProvider).setString(StorageKeys.background, name);
      await ref.read(sessionControllerProvider.notifier).refreshContext();
    } catch (_) {
      state = before;
      rethrow;
    }
  }
}
```

- [ ] **Step 4: Chạy → xanh; `flutter analyze`. Commit** `feat(settings): backgroundProvider — tai khoan thang cache, lac quan, hoan khi loi`.

### Task 9: Màn chọn nền + mục menu

**Files:**
- Create: `lib/modules/settings/presentation/background_page.dart`
- Modify: `lib/modules/settings/settings_module.dart` (hằng `background`, route, nav entry)
- Modify: `lib/modules/settings/presentation/widgets/account_menu_button.dart` (mục menu `'background'` sau `'photo'`, và `case 'background'`)
- Test: `test/settings/background_page_test.dart`

**Interfaces:**
- Consumes: `backgroundProvider` (Task 8), `OmniBackdrop`/`OmniBackdrops` (Task 3–4).
- Produces: `SettingsModule.background = 'settings.background'`, đường dẫn `/settings/background`; `BackgroundPage()`.

- [ ] **Step 1: Bài kiểm**

```dart
// test/settings/background_page_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:omni_app/core/error/app_exception.dart';
import 'package:omni_app/design/theme/omni_theme.dart';
import 'package:omni_app/design/tokens/tokens.dart';
import 'package:omni_app/modules/settings/application/appearance_providers.dart';
import 'package:omni_app/modules/settings/presentation/background_page.dart';

void main() {
  late _Stub stub;

  Widget host({String? current, bool fail = false}) {
    stub = _Stub(current, fail: fail);
    return ProviderScope(
      overrides: [backgroundProvider.overrideWith(() => stub)],
      child: MaterialApp(theme: OmniTheme.light(TargetPlatform.android), home: const BackgroundPage()),
    );
  }

  testWidgets('lưới có "Mặc định" + tám nền, đúng nhãn', (tester) async {
    await tester.pumpWidget(host());
    expect(find.text('Mặc định'), findsOneWidget);
    for (final n in OmniBackdrops.names) {
      expect(find.text(OmniBackdrops.labelOf(n)!), findsOneWidget);
    }
  });

  testWidgets('chạm một ô là đổi ngay, không có nút Lưu', (tester) async {
    await tester.pumpWidget(host());
    await tester.tap(find.text('Gỗ óc chó'));
    await tester.pump();
    expect(stub.sets, ['walnut']);
    expect(find.text('Lưu'), findsNothing);
  });

  testWidgets('chạm "Mặc định" gửi null', (tester) async {
    await tester.pumpWidget(host(current: 'sea'));
    await tester.tap(find.text('Mặc định'));
    await tester.pump();
    expect(stub.sets, [null]);
  });

  testWidgets('ô đang chọn được đánh dấu selected', (tester) async {
    await tester.pumpWidget(host(current: 'sea'));
    final tile = tester.getSemantics(find.bySemanticsLabel('Biển'));
    expect(tile.hasFlag(SemanticsFlag.isSelected), isTrue);
  });

  testWidgets('lỗi mạng thì báo, không im', (tester) async {
    await tester.pumpWidget(host(fail: true));
    await tester.tap(find.text('Nỉ búa'));
    await tester.pump();
    await tester.pump();
    expect(find.text('Mất mạng rồi'), findsOneWidget);
  });
}

class _Stub extends BackgroundController {
  _Stub(this._initial, {this.fail = false});
  final String? _initial;
  final bool fail;
  final sets = <String?>[];
  @override
  String? build() => _initial;
  @override
  Future<void> set(String? name) async {
    if (fail) throw const NetworkException('Mất mạng rồi');
    sets.add(name);
    state = name;
  }
}
```

- [ ] **Step 2: Chạy → đỏ.**

- [ ] **Step 3: Màn + route + menu**

```dart
// lib/modules/settings/presentation/background_page.dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../core/error/app_exception.dart';
import '../../../design/components/components.dart';
import '../../../design/tokens/tokens.dart';
import '../application/appearance_providers.dart';

/// Chọn nền cho cả app: lưới ô, mỗi ô là bản vẽ THẬT thu nhỏ của nền đó.
///
/// Chạm là đổi ngay — không nút Lưu: nền là thứ nhìn thấy tức thì, và một
/// nút Lưu chỉ thêm một bước giữa mắt và quyết định. Lỗi mạng thì hoàn về
/// (provider lo) và nói ra ở đây.
class BackgroundPage extends ConsumerWidget {
  const BackgroundPage({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final current = ref.watch(backgroundProvider);
    final scheme = Theme.of(context).colorScheme;

    Future<void> pick(String? name) async {
      final messenger = ScaffoldMessenger.of(context);
      try {
        await ref.read(backgroundProvider.notifier).set(name);
      } on AppException catch (e) {
        messenger.showSnackBar(SnackBar(content: Text(e.message)));
      } on Object {
        messenger.showSnackBar(
          const SnackBar(content: Text('Chưa lưu được nền. Thử lại khi có mạng.')),
        );
      }
    }

    return Scaffold(
      appBar: AppBar(title: const Text('Nền')),
      body: GridView.count(
        crossAxisCount: 2,
        padding: const EdgeInsets.fromLTRB(OmniSpacing.lg, OmniSpacing.lg, OmniSpacing.lg, OmniSpacing.bottomSafe),
        mainAxisSpacing: OmniSpacing.md,
        crossAxisSpacing: OmniSpacing.md,
        childAspectRatio: 3 / 4.6,
        children: [
          _Tile(
            label: 'Mặc định',
            selected: current == null,
            onTap: () => pick(null),
            preview: ColoredBox(color: scheme.surfaceContainerHighest),
          ),
          for (final n in OmniBackdrops.names)
            _Tile(
              label: OmniBackdrops.labelOf(n)!,
              selected: current == n,
              onTap: () => pick(n),
              preview: OmniBackdrop(name: n, child: const SizedBox.expand()),
            ),
        ],
      ),
    );
  }
}

class _Tile extends StatelessWidget {
  const _Tile({required this.label, required this.selected, required this.onTap, required this.preview});

  final String label;
  final bool selected;
  final VoidCallback onTap;
  final Widget preview;

  @override
  Widget build(BuildContext context) {
    final scheme = Theme.of(context).colorScheme;
    final text = Theme.of(context).textTheme;

    return Semantics(
      button: true,
      selected: selected,
      label: label,
      excludeSemantics: true,
      child: InkWell(
        onTap: onTap,
        borderRadius: OmniRadius.lgAll,
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Expanded(
              child: Container(
                clipBehavior: Clip.antiAlias,
                decoration: BoxDecoration(
                  borderRadius: OmniRadius.lgAll,
                  border: Border.all(
                    color: selected ? scheme.primary : scheme.outlineVariant,
                    width: selected ? 2 : 1,
                  ),
                ),
                child: Stack(
                  fit: StackFit.expand,
                  children: [
                    preview,
                    if (selected)
                      Positioned(
                        right: OmniSpacing.sm,
                        top: OmniSpacing.sm,
                        child: Icon(Icons.check_circle_rounded, color: scheme.primary),
                      ),
                  ],
                ),
              ),
            ),
            const SizedBox(height: OmniSpacing.sm),
            Text(
              label,
              style: text.labelLarge?.copyWith(
                fontWeight: selected ? FontWeight.w700 : FontWeight.w500,
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

`settings_module.dart`: thêm `import 'presentation/background_page.dart';`, hằng `static const background = 'settings.background';`, route:
```dart
    ModuleRoute(
      path: '/settings/background',
      name: background,
      rootNavigator: true,
      builder: (_, _) => const BackgroundPage(),
    ),
```
và nav entry (order 15, giữa hai mục sẵn có):
```dart
    ModuleNavEntry(
      moduleId: 'settings',
      label: 'Nền',
      subtitle: 'Chọn nền cho chat và bảng dự án',
      icon: Icons.wallpaper_outlined,
      selectedIcon: Icons.wallpaper_rounded,
      routeName: background,
      area: NavArea.account,
      weight: NavWeight.secondary,
      order: 15,
    ),
```

`account_menu_button.dart`: sau mục `'photo'` thêm
```dart
        const PopupMenuItem(
          value: 'background',
          child: ListTile(
            contentPadding: EdgeInsets.zero,
            leading: Icon(Icons.wallpaper_outlined),
            title: Text('Nền'),
          ),
        ),
```
và trong `switch (choice)`: `case 'background': context.pushNamed(SettingsModule.background);`.

- [ ] **Step 4: Chạy** `flutter test test/settings test/design/app_bar_coverage_test.dart test/architecture` → xanh; analyze sạch.

- [ ] **Step 5: Commit** `feat(settings): man chon nen + muc "Nen" trong menu tai khoan`.

### Task 10: `SurfaceBackdrop` và bọc màn chat

**Files:**
- Create: `lib/modules/settings/presentation/widgets/surface_backdrop.dart`
- Modify: `lib/modules/inbox/presentation/thread_page.dart` (bọc `body: Column(…)` tại dòng ~209)
- Test: `test/settings/surface_backdrop_test.dart`

**Interfaces:**
- Produces: `SurfaceBackdrop({required Widget child})` — ConsumerWidget đọc `backgroundProvider`, trả `OmniBackdrop(name: …, child)`.

- [ ] **Step 1: Bài kiểm**

```dart
// test/settings/surface_backdrop_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:omni_app/design/components/components.dart';
import 'package:omni_app/design/theme/omni_theme.dart';
import 'package:omni_app/modules/settings/application/appearance_providers.dart';
import 'package:omni_app/modules/settings/presentation/widgets/surface_backdrop.dart';

void main() {
  Widget host(String? name) => ProviderScope(
    overrides: [backgroundProvider.overrideWith(() => _Fixed(name))],
    child: MaterialApp(
      theme: OmniTheme.light(TargetPlatform.android),
      home: const SurfaceBackdrop(child: Text('x')),
    ),
  );

  testWidgets('đọc tên từ provider và đưa cho OmniBackdrop', (tester) async {
    await tester.pumpWidget(host('sea'));
    expect(tester.widget<OmniBackdrop>(find.byType(OmniBackdrop)).name, 'sea');
  });

  testWidgets('mặc định → OmniBackdrop nhận null (vẽ phẳng)', (tester) async {
    await tester.pumpWidget(host(null));
    expect(tester.widget<OmniBackdrop>(find.byType(OmniBackdrop)).name, isNull);
    expect(find.text('x'), findsOneWidget);
  });
}

class _Fixed extends BackgroundController {
  _Fixed(this._v);
  final String? _v;
  @override
  String? build() => _v;
}
```

- [ ] **Step 2: Chạy → đỏ.**

- [ ] **Step 3: Widget + bọc chat**

```dart
// lib/modules/settings/presentation/widgets/surface_backdrop.dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../../design/components/components.dart';
import '../../application/appearance_providers.dart';

/// Nền cả app phía sau một màn "không gian" — chat, bảng dự án.
///
/// Mảnh nối duy nhất giữa provider (module settings) và widget vẽ (design):
/// hai màn kia chỉ bọc thân mình trong đây, không màn nào phải nhớ provider.
class SurfaceBackdrop extends ConsumerWidget {
  const SurfaceBackdrop({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context, WidgetRef ref) =>
      OmniBackdrop(name: ref.watch(backgroundProvider), child: child);
}
```

`thread_page.dart`: thêm `import '../../settings/presentation/widgets/surface_backdrop.dart';` và đổi `body: Column(` thành `body: SurfaceBackdrop(child: Column(` — nhớ đóng thêm một dấu `)` ở cuối `Column(...)` tương ứng (ngay trước `),` kết thúc `body:`). `backgroundColor` của Scaffold giữ nguyên (mặc định vẫn là `chatCanvas`).

- [ ] **Step 4: Chạy** `flutter test test/settings test/inbox` → xanh; `flutter analyze` sạch. Mở app (`flutter run -d chrome --web-port=5555 …` như các lần trước), chọn "Gỗ óc chó", mở một cuộc chat: nền có vân, bong bóng vẫn nổi.

- [ ] **Step 5: Commit** `feat(inbox): man chat nhan nen ca app qua SurfaceBackdrop`.

### Task 11: Bảng dự án nhận nền + ảnh chụp

**Files:**
- Modify: `lib/modules/plans/presentation/plan_board_page.dart` (bọc `Expanded(child: PageView.builder(…))` trong `_Board`)
- Modify: `test/_screenshots/screens_golden_test.dart` (thêm màn `05b`)
- Test: `test/plans/board_backdrop_test.dart`

- [ ] **Step 1: Bài kiểm** (host y như `test/plans/plan_board_test.dart`, thêm override `backgroundProvider`)

```dart
// test/plans/board_backdrop_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:omni_app/design/components/components.dart';
import 'package:omni_app/design/theme/omni_theme.dart';
import 'package:omni_app/modules/plans/application/plans_providers.dart';
import 'package:omni_app/modules/plans/domain/plan.dart';
import 'package:omni_app/modules/plans/presentation/plan_board_page.dart';
import 'package:omni_app/modules/settings/application/appearance_providers.dart';
import 'package:omni_app/modules/tasks/domain/task.dart';

void main() {
  final plan = Plan.fromJson({
    'id': 'p1', 'name': 'Đàn cơ',
    'sections': [{'id': 's1', 'name': 'Nhập xưởng', 'order': 0}],
  });

  Widget host(String? bg) => ProviderScope(
    overrides: [
      planProvider('p1').overrideWith((ref) async => plan),
      planTasksProvider('p1').overrideWith((ref) async => (tasks: <Task>[], truncated: false)),
      backgroundProvider.overrideWith(() => _Fixed(bg)),
    ],
    child: MaterialApp(theme: OmniTheme.light(TargetPlatform.android), home: const PlanBoardPage(planId: 'p1')),
  );

  testWidgets('bảng vẽ nền đã chọn phía sau các cột', (tester) async {
    await tester.pumpWidget(host('walnut'));
    await tester.pumpAndSettle();
    final backdrop = find.byType(OmniBackdrop);
    expect(backdrop, findsOneWidget);
    expect(tester.widget<OmniBackdrop>(backdrop).name, 'walnut');
    // Nền nằm SAU cột, không sau dải nhóm việc: PageView là con của backdrop.
    expect(find.descendant(of: backdrop, matching: find.byType(PageView)), findsOneWidget);
  });

  testWidgets('mặc định thì như cũ', (tester) async {
    await tester.pumpWidget(host(null));
    await tester.pumpAndSettle();
    expect(tester.widget<OmniBackdrop>(find.byType(OmniBackdrop)).name, isNull);
  });
}

class _Fixed extends BackgroundController {
  _Fixed(this._v);
  final String? _v;
  @override
  String? build() => _v;
}
```

- [ ] **Step 2: Chạy → đỏ.**

- [ ] **Step 3: Bọc PageView**

Trong `_Board.build` thay
```dart
            Expanded(
              child: PageView.builder(
```
bằng
```dart
            Expanded(
              // Nền cả app nằm SAU các cột, không sau dải nhóm việc: dải và
              // AppBar giữ nền phẳng để đọc; thẻ việc vốn đục nên vẫn nổi.
              child: SurfaceBackdrop(
                child: PageView.builder(
```
và thêm một `)` đóng `SurfaceBackdrop(` sau `PageView.builder(...)`. Thêm `import '../../settings/presentation/widgets/surface_backdrop.dart';`.

- [ ] **Step 4: Ảnh chụp** — trong `screens_golden_test.dart` thêm ngay sau bài `'05 bang du an'`:
```dart
  testWidgets('05b bang du an - nen', (t) => shoot(
    t,
    app(const PlanBoardPage(planId: 'p1'), extra: [
      planProvider('p1').overrideWith((ref) async => _plan('p1', 'Phục chế tháng 9', cover: 'teal-1')),
      planTasksProvider('p1').overrideWith((ref) async => (tasks: _tasks.take(3).map(Task.fromJson).toList(), truncated: false)),
      backgroundProvider.overrideWith(() => _FixedBackground('walnut')),
    ]),
    '05b-bang-du-an-nen',
  ));
```
(chép đúng hai override `planProvider`/`planTasksProvider` từ bài `'05 bang du an'` hiện có nếu khác), thêm class cuối tệp:
```dart
class _FixedBackground extends BackgroundController {
  _FixedBackground(this._v);
  final String? _v;
  @override
  String? build() => _v;
}
```
và `import 'package:omni_app/modules/settings/application/appearance_providers.dart';`.

- [ ] **Step 5: Chạy** `flutter test test/plans test/settings` → xanh; `flutter test test/_screenshots --run-skipped --update-goldens`; mở `test/_screenshots/goldens/05b-bang-du-an-nen.png` bằng Read và nhìn: vân gỗ nhẹ, thẻ nổi, dải nhóm việc phẳng.

- [ ] **Step 6: Format, cả bộ, commit, push, CI**

```bash
dart format lib test && flutter analyze && flutter test --exclude-tags=live 2>&1 | tail -2
git add -A lib test && git commit -F - <<'MSG'
feat(plans): bang du an nhan nen ca app; anh chup 05b

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
git push origin main && /d/_tools/gh/bin/gh.exe run list --branch main --limit 1
```
Expected: cả bộ xanh, CI success.

---

## Phần C — Web (`d:/_omnicrm/omni-flow`)

Chạy test: `D:/_tools/node-v24.20.0-win-x64/npx.cmd vitest run <file>` (hoặc `bun run test` nếu bun có sẵn).

### Task 12: `backdrops.ts`

**Files:**
- Create: `src/lib/backdrops.ts`
- Test: `src/lib/backdrops.test.ts`

**Interfaces:**
- Produces: `BACKDROP_NAMES: readonly string[]` (8, cùng thứ tự app); `backdropLabel(name): string | null`; `backdropStyle(name: string | null | undefined, dark: boolean): CSSProperties` (`{}` khi null/lạ).

- [ ] **Step 1: Bài kiểm**

```ts
// src/lib/backdrops.test.ts
import { describe, expect, it } from "vitest";
import { BACKDROP_NAMES, backdropLabel, backdropStyle } from "./backdrops";

describe("backdrops", () => {
  it("có đúng tám tên, không trùng, cùng tên với app", () => {
    expect(BACKDROP_NAMES).toEqual(["mist", "ivory", "walnut", "felt", "brass", "graphite", "sea", "dawn"]);
  });

  it("tên hợp lệ ra gradient + hoạ tiết, bản tối khác bản sáng", () => {
    const light = backdropStyle("walnut", false);
    const dark = backdropStyle("walnut", true);
    expect(String(light.backgroundImage)).toContain("linear-gradient");
    expect(String(light.backgroundImage)).toContain("data:image/svg+xml");
    expect(light.backgroundImage).not.toEqual(dark.backgroundImage);
  });

  it("nền không hoạ tiết chỉ có gradient", () => {
    expect(String(backdropStyle("dawn", false).backgroundImage)).not.toContain("svg");
  });

  it("null hoặc tên lạ → {} và nhãn null", () => {
    expect(backdropStyle(null, false)).toEqual({});
    expect(backdropStyle("go-oc-cho", true)).toEqual({});
    expect(backdropLabel("go-oc-cho")).toBeNull();
    expect(backdropLabel("felt")).toBe("Nỉ búa");
  });
});
```

- [ ] **Step 2: Chạy → đỏ.**

- [ ] **Step 3: Bảng**

```ts
// src/lib/backdrops.ts
import type { CSSProperties } from "react";

/**
 * Bộ nền cả app — bản web của `omni_backdrops.dart` bên app.
 *
 * Là TÊN: server lưu `walnut`, mỗi client tự dịch ra gradient của mình. Thứ
 * ba kho phải khớp là danh sách tên (app có bài kiểm đọc tệp này). Màu theo
 * đúng bảng bên app; hoạ tiết là SVG data-URI lặp, alpha 0.06.
 */
type Pattern = "none" | "dots" | "diagonal" | "grain";
interface Spec { top: string; bottom: string; pattern: Pattern }
interface Backdrop { label: string; light: Spec; dark: Spec }

const BACKDROPS: Record<string, Backdrop> = {
  mist: { label: "Sương sớm", light: { top: "#EEF6F4", bottom: "#E3EFEB", pattern: "dots" }, dark: { top: "#15211F", bottom: "#0F1817", pattern: "dots" } },
  ivory: { label: "Ngà phím", light: { top: "#F9F6EE", bottom: "#F2ECDE", pattern: "diagonal" }, dark: { top: "#232019", bottom: "#191712", pattern: "diagonal" } },
  walnut: { label: "Gỗ óc chó", light: { top: "#F5ECE4", bottom: "#EEE0D3", pattern: "grain" }, dark: { top: "#2A1F18", bottom: "#1C1511", pattern: "grain" } },
  felt: { label: "Nỉ búa", light: { top: "#F8EDED", bottom: "#F1E0E0", pattern: "dots" }, dark: { top: "#2B1A1C", bottom: "#1E1214", pattern: "dots" } },
  brass: { label: "Dây đồng", light: { top: "#F7F2E6", bottom: "#EFE7D2", pattern: "diagonal" }, dark: { top: "#2A2416", bottom: "#1B1810", pattern: "diagonal" } },
  graphite: { label: "Than chì", light: { top: "#F0F2F4", bottom: "#E5E9ED", pattern: "dots" }, dark: { top: "#1B1F24", bottom: "#11141A", pattern: "dots" } },
  sea: { label: "Biển", light: { top: "#EBF3F8", bottom: "#DFEAF1", pattern: "grain" }, dark: { top: "#14222B", bottom: "#0E181F", pattern: "grain" } },
  dawn: { label: "Rạng đông", light: { top: "#FAF1EB", bottom: "#F0E7F0", pattern: "none" }, dark: { top: "#2A1E24", bottom: "#1A141C", pattern: "none" } },
};

export const BACKDROP_NAMES: readonly string[] = Object.keys(BACKDROPS);

export function backdropLabel(name: string | null | undefined): string | null {
  return name ? (BACKDROPS[name]?.label ?? null) : null;
}

const ALPHA = 0.06;

function patternSvg(pattern: Pattern, dark: boolean): string | null {
  const c = dark ? "#fff" : "#000";
  switch (pattern) {
    case "dots":
      return `<svg xmlns='http://www.w3.org/2000/svg' width='24' height='24'><circle cx='12' cy='12' r='1' fill='${c}' fill-opacity='${ALPHA}'/></svg>`;
    case "diagonal":
      return `<svg xmlns='http://www.w3.org/2000/svg' width='28' height='28'><path d='M0 28L28 0' stroke='${c}' stroke-opacity='${ALPHA}' stroke-width='1'/></svg>`;
    case "grain":
      return `<svg xmlns='http://www.w3.org/2000/svg' width='64' height='18'><path d='M0 9Q16 6 32 9T64 9' fill='none' stroke='${c}' stroke-opacity='${ALPHA}'/></svg>`;
    case "none":
      return null;
  }
}

/** Style áp lên khung chat / vùng bảng. `{}` cho null hoặc tên lạ. */
export function backdropStyle(name: string | null | undefined, dark: boolean): CSSProperties {
  const b = name ? BACKDROPS[name] : undefined;
  if (!b) return {};
  const s = dark ? b.dark : b.light;
  const layers = [`linear-gradient(180deg, ${s.top} 0%, ${s.bottom} 100%)`];
  const svg = patternSvg(s.pattern, dark);
  if (svg) layers.unshift(`url("data:image/svg+xml,${encodeURIComponent(svg)}")`);
  return { backgroundImage: layers.join(", ") };
}
```
Bài kiểm khớp tên bên app đọc mẫu `  walnut: {` — giữ đúng hai khoảng trắng thụt đầu dòng và dấu `{` như trên.

- [ ] **Step 4: Chạy → xanh**; sang app chạy `flutter test test/design/backdrop_names_match_web_test.dart` → xanh (không còn skipped).

- [ ] **Step 5: Commit** `feat(lib): backdrops — bo nen ca app, cung ten voi app`.

### Task 13: Phiên web mang `background`; API ghi

**Files:**
- Modify: `src/modules/auth/domain/types.ts` (`AuthUser` thêm `appearance?: { background?: string | null } | null;`)
- Modify: `src/lib/store.ts` (`AppUser` thêm `background?: string | null;`)
- Modify: `src/lib/auth-session.ts` (trong `setAuthSession({ user: { … avatar … } })` thêm `background: apiUser?.appearance?.background ?? null,`)
- Modify: `src/modules/auth/application/auth-api.ts` (thêm hàm)
- Test: `src/lib/auth-session.test.ts` (nếu có hàm thuần dựng user thì thêm bài; nếu không, bài kiểm nằm ở `backdrops.test.ts` đã đủ cho phần thuần — phần này kiểm bằng kiểu TypeScript + `tsc`)

- [ ] **Step 1: Sửa bốn tệp**

`auth-api.ts` thêm:
```ts
/** Ghi nền cả app lên tài khoản. `null` = mặc định, vẫn phải GỬI khoá. */
export async function updateBackground(background: string | null): Promise<void> {
  await apiFetch("/auth/appearance", { method: "PUT", body: JSON.stringify({ background }) });
}
```

- [ ] **Step 2: `npx tsc --noEmit` (hoặc `bun run typecheck` nếu có) → sạch; `vitest run` → xanh.**

- [ ] **Step 3: Commit** `feat(auth): phien web mang appearance.background; updateBackground`.

### Task 14: Menu "Nền" ở Topbar

**Files:**
- Modify: `src/components/app/Topbar.tsx` (menu tài khoản: thêm `DropdownMenuSub` trước mục đăng xuất)
- Modify: `src/locales/vi/common.json`, `src/locales/en/common.json` (trong nhóm `"topbar"`: `"background": "Nền"` / `"Background"`, `"backgroundDefault": "Mặc định"` / `"Default"`)

- [ ] **Step 1: Sửa Topbar**

Import thêm từ `@/components/ui/dropdown-menu`: `DropdownMenuSub, DropdownMenuSubTrigger, DropdownMenuSubContent`; từ `@/lib/backdrops`: `BACKDROP_NAMES, backdropLabel, backdropStyle`; từ `../../modules/auth/application/auth-api`: `updateBackground`; icon `Wallpaper` từ `lucide-react`. Trong component lấy `const theme = useApp((s) => s.theme); const setUser = useApp((s) => s.setUser);`.

Chèn trước `<DropdownMenuItem onClick={handleLogout}>`:
```tsx
              <DropdownMenuSub>
                <DropdownMenuSubTrigger>
                  <Wallpaper className="size-4 mr-2" />
                  {t("topbar.background")}
                </DropdownMenuSubTrigger>
                <DropdownMenuSubContent className="w-64 p-2">
                  <div className="grid grid-cols-3 gap-2">
                    {[null, ...BACKDROP_NAMES].map((name) => {
                      const selected = (user?.background ?? null) === name;
                      return (
                        <button
                          key={name ?? "default"}
                          type="button"
                          aria-pressed={selected}
                          onClick={() => pickBackground(name)}
                          className={cn(
                            "rounded-lg border p-1 text-[11px] text-left",
                            selected ? "border-primary ring-2 ring-primary/40" : "border-border",
                          )}
                        >
                          <div
                            className="h-10 w-full rounded-md bg-muted"
                            style={backdropStyle(name, theme === "dark")}
                          />
                          <span className="mt-1 block truncate">
                            {name ? backdropLabel(name) : t("topbar.backgroundDefault")}
                          </span>
                        </button>
                      );
                    })}
                  </div>
                </DropdownMenuSubContent>
              </DropdownMenuSub>
              <DropdownMenuSeparator />
```
và hàm trong component:
```tsx
  const pickBackground = async (name: string | null) => {
    if (!user) return;
    const before = user.background ?? null;
    // Lạc quan: đổi ngay, lỗi thì trả về.
    setUser({ ...user, background: name });
    try {
      await updateBackground(name);
    } catch {
      setUser({ ...user, background: before });
    }
  };
```

- [ ] **Step 2: `npx tsc --noEmit` sạch, `vitest run` xanh, mở web local (http://localhost:5173) → menu avatar → "Nền" → chọn: nền đổi ngay ở chat/bảng (sau Task 15).**

- [ ] **Step 3: Commit** `feat(topbar): menu "Nen" — chon nen ca app, lac quan`.

### Task 15: Chat và bảng web nhận nền

**Files:**
- Modify: `src/modules/inbox/ui/app/chat-thread.tsx` (khung cuộn tin nhắn, dòng ~581: `<div className="flex-1 min-h-0 overflow-y-auto scrollbar-thin p-3 md:p-4 space-y-3 bg-[var(--chat-bg)]">`)
- Modify: `src/modules/tasks/ui/app/project-board-page.tsx` (hai khung `flex-1 min-h-0 px-4 md:px-6 pt-4 pb-6 overflow-x-auto scrollbar-thin`, dòng ~652 và ~701)

- [ ] **Step 1: Áp style**

Ở mỗi tệp: `import { backdropStyle } from "@/lib/backdrops";` và trong component `const background = useApp((s) => s.user?.background ?? null); const dark = useApp((s) => s.theme === "dark");` (import `useApp` từ `@/lib/store` nếu chưa). Thêm `style={backdropStyle(background, dark)}` vào các `div` nêu trên. Ở chat, khi có nền thì bỏ `bg-[var(--chat-bg)]` bằng `cn("…", !background && "bg-[var(--chat-bg)]")`.

- [ ] **Step 2: `npx tsc --noEmit`, `vitest run`, mở web: chọn "Biển" → chat và bảng có nền, "Mặc định" → như cũ. Đổi nền trên app (:5555) rồi tải lại web → cùng nền.**

- [ ] **Step 3: Commit, push, CI**

```bash
git add -A src && git commit -F - <<'MSG'
feat(inbox,tasks): chat va bang du an nhan nen ca app

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
MSG
git push origin main && /d/_tools/gh/bin/gh.exe run list --branch main --limit 1
```
Expected: CI success.

---

## Self-review (đã chạy khi viết)

- **Spec coverage**: §1 → Task 3, 4, 12; §2 → Task 1, 2, 6, 7, 8, 13; §3 → Task 10, 11, 15; §4 → Task 9, 14; §5 → mọi task có bài kiểm; ảnh chụp chat trong spec thay bằng kiểm tay (spec đã sửa cùng commit). §6/§7 tôn trọng.
- **Placeholder**: không có TBD/"tương tự Task N"; mọi bước mã có mã.
- **Type consistency**: `backgroundProvider`/`BackgroundController.set(String?)` (Task 8) dùng ở Task 9, 10, 11; `OmniBackdrops.specOf(name, Brightness)` (Task 3) dùng ở Task 4; `backdropStyle(name, dark)` (Task 12) dùng ở Task 14, 15; `SessionUser.background` (Task 6) dùng ở Task 8; `Backdrops::NAMES` (Task 1) dùng ở Task 2.
