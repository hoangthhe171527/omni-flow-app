/// Tên route của module kế hoạch. Xem `tasks/routes.dart` về lý do file này
/// không import gì.
library;

abstract final class PlanRoutes {
  static const teams = 'plans.teams';
  static const board = 'plans.board';
  static const timeline = 'plans.timeline';
}
